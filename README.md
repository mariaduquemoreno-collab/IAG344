# Clase IA (titulo)
## comandos de consala (subtitulo)
### ![alt text](image.png) 
``` 
comillas para cuadro de texto 

python -- version (verison de python)
python -m venv env3.14.2 (para crear un ambiente virtual)
pip list (me muestra las librerias que tengo)
cd (para localizarse entre carpetas)
dir (verficar si estoy en el entorno)
```
![alt text](image-2.png)
```
git --version (version de git)
git init ( solo se hace una vez por carpeta, crea una carpeta oculta)
git add . (añade todos los datos)
git commint -m "nombre del commit" (guardad en la carpeta)
git push(actualizar)
```
![alt text](image-3.png)
```
git branch -M main
git remote add origin https://github.com/mariaduquemoreno-collab/IAG344.git (para que el sepa que donde se van a guardar las cosas es el link dado.)
git push -u origin master(primer push que se pone despues se pone el pus solo)
```
